# copy to .env.local and fill in. Vite exposes only VITE_-prefixed vars.
VITE_PRIVY_APP_ID=        # from privy.io dashboard (or the co-hacker's app)
VITE_RELAYER_URL=http://localhost:8787
VITE_CLUSTER=devnet       # devnet | mainnet
