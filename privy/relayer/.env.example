# copy to .env (chmod 600) and fill in
CLUSTER=devnet                 # devnet | mainnet
RELAYER_SECRET_KEY=            # base58 OR json array — the fee-payer/rent-gift keypair (npm run keygen)
PORT=8787
MAX_PER_MIN=60                 # rate limit on rent-gift sponsorship
